{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "ai:dev": "cd artifacts/ai-service && ./run.sh",
    "api:dev": "export PORT=5001 && pnpm --filter @workspace/api-server run dev",
    "ui:dev": "export PORT=3000 && pnpm --filter @workspace/facesync run dev",
    "dev": "pnpm run api:dev & pnpm run ui:dev & pnpm run ai:dev"
  },
  "private": true,
  "devDependencies": {
    "@rollup/rollup-darwin-arm64": "^4.60.2",
    "drizzle-kit": "^0.31.9",
    "lightningcss-darwin-arm64": "^1.32.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "@types/pg": "^8.18.0",
    "drizzle-orm": "catalog:",
    "motion-dom": "^12.38.0",
    "pg": "^8.20.0"
  }
}
